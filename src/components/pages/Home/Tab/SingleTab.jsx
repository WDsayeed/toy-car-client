import { Tab, TabList, TabPanel } from "react-tabs";

const SingleTab = ({data, handleTabClick}) => {
  const {
          photo,
          name,
          seller,
          rating,
          quantity,
          description,
          email,
          price,
        } = data

  return (
    <div></div>
  );
};

export default SingleTab;
